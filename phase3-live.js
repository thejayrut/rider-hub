/* Consolidated Rider Hub changes approved after screenshot review. */
(()=>{
const FILE_ACCEPT='.pdf,.jpg,.jpeg,.png,.webp,.heic,.heif,.tif,.tiff,.gif,.bmp,.doc,.docx,.odt,.rtf,.txt,application/pdf,image/jpeg,image/png,image/webp,image/heic,image/heif,image/tiff,image/gif,image/bmp,application/msword,application/vnd.openxmlformats-officedocument.wordprocessingml.document,application/vnd.oasis.opendocument.text,text/plain';
let installPrompt=null;
window.addEventListener('beforeinstallprompt',e=>{e.preventDefault();installPrompt=e});
function ensureState(){state.ui=state.ui||{};if(!['list','detail'].includes(state.ui.rideView))state.ui.rideView='list';state.ride.customEmergency=Array.isArray(state.ride.customEmergency)?state.ride.customEmergency:[];state.ride.customEmergency=state.ride.customEmergency.map(x=>typeof x==='string'?parseLegacyContact(x):x).filter(x=>x&&x.number).slice(0,3);state.customRides=Array.isArray(state.customRides)?state.customRides:[]}
function parseLegacyContact(x){const s=String(x||'').trim();if(!s)return null;const m=s.match(/^(.*?)(?:\s*[-–:]\s*)?(\+?\d[\d\s-]{6,})$/);return m?{name:(m[1]||'Emergency contact').trim(),number:m[2].replace(/[^+\d]/g,'')}:{name:'Emergency contact',number:s.replace(/[^+\d]/g,'')}}
function applyChrome(){
 const brand=document.querySelector('.brandname'),sub=document.querySelector('.brand .sub'),mark=document.querySelector('.brandmark');if(brand)brand.innerHTML='TVS <em>RONIN</em>';if(sub)sub.textContent='CHARCOAL EMBER';if(mark){mark.title='My Account';mark.setAttribute('aria-label','Open My Account');mark.onclick=openMyAccount}
 const main=document.querySelector('main');if(main&&!document.querySelector('#more')){const s=document.createElement('section');s.id='more';s.className='page';main.appendChild(s)}
 const old=document.querySelector('.navbtn[data-page="docs"]');if(old){old.dataset.page='more';old.innerHTML='<b>•••</b><span>More</span>';old.onclick=()=>setPage('more')}
}
function docsHtml(){return `<div class="section-head"><div><div class="kicker">DOCUMENTS</div><h2>Document vault</h2></div></div><div class="card list">${DOCS.map(x=>`<div class="listrow" onclick="openDocDetail('${x.id}')" style="cursor:pointer"><div class="ico">▤</div><div><strong>${esc(x.name)}</strong><p>${esc(x.summary)}</p></div><span class="pill good">SAVED</span></div>`).join('')}</div><div class="section-head"><div><div class="kicker">DIGILOCKER</div><h2>Verified documents</h2></div></div><div class="card coming-card"><strong>COMING SOON</strong></div>`}
window.renderBike=function(){ensureState();const b=state.bike,c=chainKm();$('#bike').innerHTML=`<div class="card bike-hero"><div class="kicker">MY MOTORCYCLE</div><h1>${b.name}</h1><p>${b.trim}</p><div class="metrics"><div class="metric odo-metric"><label>MASTER ODOMETER</label><div class="odo-inline"><b>${b.odo.toLocaleString('en-IN')} km</b><button class="odo-edit" onclick="editOdo()" aria-label="Edit odometer" title="Edit odometer">✎</button></div></div><div class="metric"><label>OWNED SINCE</label><b>${b.purchase}</b></div></div></div><div class="section-head"><div><div class="kicker">OVERVIEW</div><h2>Bike information</h2></div></div><div class="bike-info-grid">${[['Manufacturer',b.manufacturer],['Model',b.model],['Variant',b.variant],['Colour',b.colour],['Year',b.year],['Engine',b.engine],['Braking',b.braking],['Transmission',b.transmission],['Fuel tank',b.fuelTank],['Tyres',b.tyres],['Insurance',b.insurance],['First service',`${b.firstService.odo} km · ${b.firstService.date} · ${money(b.firstService.cost)}`],['Next service',`${b.nextServiceKm.toLocaleString('en-IN')} km or ${b.nextServiceDate}`]].map(x=>`<div class="info"><label>${String(x[0]).toUpperCase()}</label><b>${x[1]}</b></div>`).join('')}</div><div class="section-head"><div><div class="kicker">MAINTENANCE</div><h2>Maintenance engine</h2></div></div><div class="card list"><div class="listrow"><div class="ico">⛓</div><div><strong>Chain clean + lube</strong><p>${c} km since last care. Manufacturer interval: 500 km. Reminder begins at 400 km.</p></div><span class="pill ${c>500?'due':c>=400?'soon':'good'}">${c>500?'OVERDUE':c>=400?'SOON':'GOOD'}</span></div><div class="listrow"><div class="ico">🔧</div><div><strong>Second service</strong><p>5,500–6,000 km or 6 months, whichever comes first.</p></div><span class="pill soon">UPCOMING</span></div><div class="listrow"><div class="ico">🛞</div><div><strong>Tyre pressure</strong><p>Check weekly and before long rides.</p></div><span class="pill good">WEEKLY</span></div></div><button class="maintenance-more" onclick="openMaintenanceMore()">More maintenance <span>›</span></button><div class="section-head"><div><div class="kicker">SERVICE HISTORY</div><h2>Completed work</h2></div></div><div class="card list">${(b.serviceHistory||[]).map(x=>`<div class="listrow"><div class="ico">✓</div><div><strong>${esc(x.name)}</strong><p>${esc(x.date)} · ${x.odo} km · ${esc(x.work)}</p></div><b>${money(x.cost)}</b></div>`).join('')}</div>${docsHtml()}`}
window.openMaintenanceMore=function(){const items=[
 ['Service intervals','1st: 750–1,000 km / 30–45 days · 2nd: 5,500–6,000 km / 6 months · 3rd: 11,500–12,000 km / 12 months · then every 6,000 km / 6 months.'],
 ['Engine + transmission oil','Replace at every scheduled service.'],['Oil filter strainer','Clean at every scheduled service.'],['Oil filter element','Replace at every scheduled service.'],['Spark plug','Replace every 12,000 km.'],['Air cleaner element','Replace every 12,000 km; earlier in dusty conditions.'],['Tappet clearance','Inspect at every scheduled service.'],['Clutch + throttle play','Inspect and adjust at every scheduled service.'],['Steering','Inspect at every service; adjust if required and lubricate every 24,000 km.'],['Front fork oil','Replace every 36,000 km if required for poor function or oil leakage.'],['Front + rear suspension','Inspect for proper functioning at every service.'],['Fasteners','Inspect and tighten at every service.'],['Drive chain','Clean, lubricate and adjust at every service; rider care every 500 km.'],['Chain guide bottom','Inspect and replace if required.'],['Bulbs, horn + switches','Inspect at every service.'],['Battery voltage + terminals','Inspect at every service.'],['Brake effectiveness / leaks','Inspect at every service.'],['Brake pedal shaft','Lubricate at every service.'],['Brake pads, hoses, WSS clamps + brake fluid','Inspect at every service. Replace brake fluid every 21,000 km or 2 years, whichever is earlier.'],['MIL / diagnostics','Scan if the malfunction indicator lamp remains on.'],['Wheel freeness','Inspect at every service and rotate/check weekly.'],['Tyre pressure','Inspect at every service; rider check weekly.'],['Centre + side stand pivots','Lubricate at every service.'],['Swing-arm bearings','Clean and lubricate every 24,000 km.'],['Wheel balancing','Inspect and adjust every 12,000 km.']];openModal(`<div class="modalhead"><div><div class="kicker">TVS RONIN</div><h3>Full maintenance schedule</h3><p class="caption">Kilometres or time — whichever comes first.</p></div><button class="round" onclick="closeModal()">×</button></div><div class="maintenance-full">${items.map((x,i)=>`<div class="maintenance-item"><div class="maintenance-index">${String(i+1).padStart(2,'0')}</div><div><strong>${x[0]}</strong><p>${x[1]}</p></div></div>`).join('')}</div>`)}
const phase2Gear=window.renderGear;window.renderGear=function(){phase2Gear();const p=document.querySelector('#gear .gear-hero p');if(p)p.textContent='Owned gear, planned purchases, bike setup, luggage, electronics and care items — all in one garage.';const note=document.querySelector('#gear .data-note');if(note)note.textContent='Prices marked unknown are intentionally left blank rather than guessed.'}
function rideOverallProgress(){let total=0,done=0;days.forEach(d=>d.tasks.forEach((_,i)=>{total++;if(['done','skipped'].includes(state.ride.tasks[d.day+'_'+i]||'upcoming'))done++}));return total?Math.round(done/total*100):0}
function rideCompleted(){return days.every(d=>d.tasks.every((_,i)=>['done','skipped'].includes(state.ride.tasks[d.day+'_'+i]||'upcoming')))}
function renderRideList(){const done=rideCompleted(),pct=rideOverallProgress(),custom=state.customRides||[],up=(done?0:1)+custom.length;$('#rides').innerHTML=`<div class="section-head ride-list-head"><div><div class="kicker">MY RIDES</div><h2>Plan. Ride. Remember.</h2></div><button class="add-ride-btn" onclick="openSimpleRideAdd()">+ Add ride</button></div><div class="ride-summary"><div class="card stat"><label>UPCOMING</label><b>${up}</b><small>planned</small></div><div class="card stat"><label>COMPLETED</label><b>${done?1:0}</b><small>rides</small></div><div class="card stat"><label>TOURING</label><b>0</b><small>km logged</small></div></div><div style="margin-top:12px"><div class="card ride-card" onclick="openRideFromList()"><div class="ride-card-top"><div><div class="kicker">OFFLINE READY</div><h3>Banswara 3-Day Ronin Adventure</h3><div class="ride-route">Ahmedabad → Banswara → Ahmedabad</div></div><span class="ride-status">${done?'COMPLETED':'UPCOMING'}</span></div><div class="ride-meta"><span>28–30 Aug 2026</span><span>2 riders</span><span>3 days</span></div><div class="ride-progress"><i style="width:${pct}%"></i></div></div>${custom.map(r=>`<div class="card ride-card" onclick="openCustomRide('${r.id}')"><div class="ride-card-top"><div><div class="kicker">PLANNED</div><h3>${esc(r.name)}</h3><div class="ride-route">${esc(r.route||'Route not added')}</div></div><span class="ride-status">UPCOMING</span></div><div class="ride-meta"><span>${esc(r.start||'Date TBD')}${r.end&&r.end!==r.start?' → '+esc(r.end):''}</span></div></div>`).join('')}</div>`}
function renderRideDetail(){const d=currentDay(),p=dayProgress();$('#rides').innerHTML=`<button class="ride-list-back" onclick="backToRideList()">‹ My rides</button><div class="card ride-hero"><div class="kicker">BANSWARA RIDE</div><h1>Banswara 3-Day Ronin Adventure</h1><p>28–30 Aug 2026 · 2 riders · TVS Ronin</p><div class="ride-mode-cta"><button class="primary" onclick="openRideMode()">Open Ride Mode · Day ${d.day}</button><div class="countdown"><div class="kicker">NEXT</div><b id="nextTaskTime">—</b></div></div><button class="secondary full" onclick="addNextTaskToCalendar()">Add next task to Google Calendar</button></div><div id="weatherCard" class="card weather"><div class="kicker">LIVE WEATHER</div><strong>Loading…</strong></div><div class="card booking" style="margin-top:10px"><div class="row between"><div><div class="kicker">STAY CONFIRMED</div><h3>Hotel Landmark, Banswara</h3><div class="caption">28–30 Aug · 2 nights · Deluxe Room · ₹2,470 paid</div></div><span class="pill good">PAID</span></div><div class="booking-actions"><button class="secondary" onclick="openBookingPdf()">Open booking</button><button class="secondary" onclick="openHotelMap()">Map hotel</button></div></div><div class="section-head"><div><div class="kicker">CHOOSE DAY</div><h2>Ride plan</h2></div></div><div class="daytabs">${days.map((x,i)=>`<button class="daytab ${i===state.ride.selectedDay?'active':''}" onclick="selectDay(${i})">Day ${x.day} · ${x.from} → ${x.to}</button>`).join('')}</div><div class="card" style="padding:14px;margin-top:8px"><div class="row between"><div><div class="kicker">DAY ${d.day}</div><strong>${d.title}</strong></div><span class="pill soon">${p}% DONE</span></div><div class="progress" style="margin:12px 0"><i style="width:${p}%"></i></div><div class="timeline">${d.tasks.map((t,i)=>taskRow(t,i)).join('')}</div></div><div class="section-head"><div><div class="kicker">TRIP TOOLKIT</div><h2>Ride operations</h2></div></div><div class="toolgrid ride-toolkit">${[['📝','Notes','openNotes()'],['⚠','Bike issue','openIssue()'],['◉','Content','openChecklist("content")'],['☁','Media backup','openChecklist("backup")'],['▣','Packing','openChecklist("packing")'],['→','Tomorrow','openTomorrow()'],['SOS','Emergency','openEmergency()'],['↕','Traffic','openTraffic()'],['Σ','Ride Summary','openSummary()']].map(x=>`<button class="tool" onclick='${x[2]}'><b>${x[0]}</b><span>${x[1]}</span></button>`).join('')}</div>`;renderWeather();renderCountdown()}
window.renderRides=function(){ensureState();state.ui.rideView==='detail'?renderRideDetail():renderRideList()}
window.openRideFromList=function(){state.ui.rideView='detail';history.pushState({page:'rides',rideView:'detail'},'',location.pathname+'#rides');renderRideDetail()}
window.backToRideList=function(){state.ui.rideView='list';if(history.state?.rideView==='detail')history.back();else renderRideList()}
window.openSimpleRideAdd=function(){openModal(`<div class="modalhead"><div><div class="kicker">ADD RIDE</div><h3>New ride</h3></div><button class="round" onclick="closeModal()">×</button></div><div class="field"><label>RIDE NAME</label><input id="newRideName" placeholder="Weekend ride"></div><div class="field"><label>ROUTE</label><input id="newRideRoute" placeholder="Ahmedabad → destination"></div><div class="grid2"><div class="field"><label>START</label><input id="newRideStart" type="date"></div><div class="field"><label>END</label><input id="newRideEnd" type="date"></div></div><div class="modal-actions"><button class="secondary" onclick="closeModal()">Cancel</button><button class="confirm" onclick="saveSimpleRide()">✓ Add ride</button></div>`)}
window.saveSimpleRide=function(){const name=$('#newRideName')?.value.trim();if(!name)return toast('Add a ride name');snapshot('add ride');state.customRides=state.customRides||[];state.customRides.push({id:'ride_'+Date.now().toString(36),name,route:$('#newRideRoute')?.value.trim()||'',start:$('#newRideStart')?.value||'',end:$('#newRideEnd')?.value||''});save();closeModal();renderRideList();toast('Ride added')}
window.openCustomRide=function(id){const r=(state.customRides||[]).find(x=>x.id===id);if(!r)return;openModal(`<div class="modalhead"><div><div class="kicker">PLANNED RIDE</div><h3>${esc(r.name)}</h3><p class="caption">${esc(r.route||'Route not added')}</p></div><button class="round" onclick="closeModal()">×</button></div><div class="grid2"><div class="tile"><label>START</label><b>${esc(r.start||'TBD')}</b></div><div class="tile"><label>END</label><b>${esc(r.end||r.start||'TBD')}</b></div></div>`)}
const baseRenderRideMode=window.renderRideMode;window.renderRideMode=function(){const d=currentDay(),n=nextOpenTask(),t=n?n.t:['—','Day complete','All tasks done.'];$('#rideModeInner').innerHTML=`<div class="header ride-mode-header"><div><div class="kicker">RIDE MODE · DAY ${d.day}</div><div class="sub">${d.from} → ${d.to}</div></div><div class="header-actions"><button class="round" onclick="undo()" ${undoStack.length?'':'disabled'}>↶</button><button class="round" onclick="closeRideMode()">×</button></div></div><div class="now"><div class="kicker">${t[0]} · CURRENT</div><h2>${esc(t[1])}</h2><p class="caption">${esc(t[2])}</p><div class="progress" style="margin-top:12px"><i style="width:${dayProgress()}%"></i></div></div>${n?`<div class="action3"><button class="confirm" onclick="setRideModeTask(${n.i},'done')">Done</button><button class="secondary" onclick="delayTask(${n.i})">Delay</button><button class="secondary" onclick="setRideModeTask(${n.i},'skipped')">Skip</button></div>`:''}<div class="toolgrid ride-mode-tools" style="margin-top:10px"><button class="tool" onclick="openRideModeMapMenu()"><b>⌖</b><span>Map</span></button><button class="tool" onclick="openExpenses()"><b>₹</b><span>Expenses</span></button><button class="tool" onclick="openFuel()"><b>⛽</b><span>Fuel log</span></button><button class="tool" onclick="openEmergency()"><b>SOS</b><span>Emergency</span></button><button class="tool" onclick="openSummary()"><b>Σ</b><span>Ride Summary</span></button></div><div class="section-head"><div><div class="kicker">CHECKS</div><h2>Pre-ride checks</h2></div></div><div class="card" style="padding:12px">${[['tyres','Tyres','Pressure, condition, puncture kit'],['luggage','Luggage','Straps, balance, exhaust clearance'],['electronics','Electronics','Phone, maps, intercom, Action 5'],['pillion','Pillion essentials','Water, gear fit and rest plan']].map(x=>`<div class="checkrow"><div><strong>${x[1]}</strong><small>${x[2]}</small></div><button class="checkbtn ${state.ride.preflight[d.day+'_'+x[0]]?'on':''}" onclick="togglePreflight('${x[0]}')">✓</button></div>`).join('')}</div><div class="section-head"><div><div class="kicker">DAY TIMELINE</div><h2>Schedule</h2></div></div><div class="card" style="padding:14px"><div class="timeline">${d.tasks.map((x,i)=>taskRow(x,i)).join('')}</div></div>`}
window.openRideModeMapMenu=function(){const d=currentDay();openModal(`<div class="modalhead"><div><div class="kicker">DAY ${d.day} MAP</div><h3>Route parts</h3></div><button class="round" onclick="closeModal()">×</button></div>${d.segments.map((s,i)=>`<div class="routecard"><div class="row between"><strong>${esc(s.label)}</strong><button class="secondary" onclick="openRoute(${i},false)">Open</button></div><p>${[s.origin,...s.waypoints,s.destination].map(esc).join(' → ')}</p></div>`).join('')}${d.backup?.length?`<div class="section-head compact"><div><div class="kicker">WET WEATHER</div><h2>Backup route</h2></div></div>${d.backup.map((s,i)=>`<div class="routecard"><div class="row between"><strong>${esc(s.label)}</strong><button class="secondary" onclick="openRoute(${i},true)">Open</button></div><p>${[s.origin,...s.waypoints,s.destination].map(esc).join(' → ')}</p></div>`).join('')}`:''}`)}
window.openTomorrow=function(){const n=days[state.ride.selectedDay+1];if(!n)return openModal(`<div class="modalhead"><div><div class="kicker">TOMORROW</div><h3>Trip complete</h3></div><button class="round" onclick="closeModal()">×</button></div><div class="alert">No next ride day.</div>`);const majors=n.tasks.filter(x=>/Chacha|Mahi|Singhpura|Jagmer|Arthuna|Hotel|Ahmedabad|Mangarh/i.test(x[1])).slice(0,5);openModal(`<div class="modalhead"><div><div class="kicker">TOMORROW</div><h3>${esc(n.title)}</h3><p class="caption">Day ${n.day} · ${esc(n.from)} → ${esc(n.to)}</p></div><button class="round" onclick="closeModal()">×</button></div><div class="grid2"><div class="tile"><label>START</label><b>${esc(n.tasks[0]?.[0]||'—')}</b></div><div class="tile"><label>DAY END</label><b>${esc(n.end||'—')}</b></div></div><div class="routecard"><strong>Key stops</strong><p>${majors.length?majors.map(x=>esc(x[1])).join(' → '):'Ride plan ready.'}</p></div><div class="routecard"><strong>Prepare tonight</strong><p>Fuel · rain layer · charged phone/intercom/camera · documents · water · tyre/chain check.</p></div>`)}
function contacts(){ensureState();return state.ride.customEmergency}
window.openEmergency=function(){const c=contacts();openModal(`<div class="modalhead"><div><div class="kicker">EMERGENCY</div><h3>Emergency actions</h3></div><button class="round" onclick="closeModal()">×</button></div><div class="grid2"><button class="primary" onclick="location.href='tel:112'">Call 112</button><button class="secondary" onclick="location.href='tel:108'">Ambulance 108</button></div><button class="secondary full" style="margin-top:8px" onclick="shareLocation()">Share current location</button><div class="emergency-contacts">${c.map((x,i)=>`<div class="contact-card"><button class="contact-call" onclick="callEmergencyContact(${i})"><span><strong>${esc(x.name)}</strong><small>${esc(x.number)}</small></span><b>Call</b></button><div class="contact-actions"><button onclick="editEmergencyContact(${i})">Edit</button><button onclick="deleteEmergencyContact(${i})">Delete</button></div></div>`).join('')||'<div class="caption empty-note">No custom contacts yet.</div>'}</div>${c.length<3?'<button class="secondary full" style="margin-top:10px" onclick="editEmergencyContact(-1)">+ Add contact</button>':''}`)}
window.editEmergencyContact=function(i){const x=i>=0?contacts()[i]:{name:'',number:''};openModal(`<div class="modalhead"><div><div class="kicker">EMERGENCY CONTACT</div><h3>${i>=0?'Edit contact':'Add contact'}</h3></div><button class="round" onclick="openEmergency()">×</button></div><div class="field"><label>NAME</label><input id="emName" value="${esc(x.name)}" placeholder="Name"></div><div class="field"><label>PHONE NUMBER</label><input id="emNumber" inputmode="tel" value="${esc(x.number)}" placeholder="+91…"></div><div class="modal-actions"><button class="secondary" onclick="openEmergency()">Cancel</button><button class="confirm" onclick="saveEmergencyContact(${i})">✓ Save</button></div>`)}
window.saveEmergencyContact=function(i){const name=$('#emName')?.value.trim(),number=$('#emNumber')?.value.replace(/[^+\d]/g,'');if(!name||!number||number.replace(/\D/g,'').length<7)return toast('Add a valid name and phone number');snapshot('emergency contact');const c=contacts();if(i>=0)c[i]={name,number};else if(c.length<3)c.push({name,number});save();openEmergency();toast('Emergency contact saved')}
window.deleteEmergencyContact=function(i){snapshot('delete emergency contact');state.ride.customEmergency.splice(i,1);save();openEmergency();toast('Contact removed')}
window.callEmergencyContact=function(i){const x=contacts()[i];if(x?.number)location.href='tel:'+x.number}
const baseMapsSetup=window.openMapsDemoSetup;window.openMapsDemoSetup=function(after=''){const existing=localStorage.getItem('riderhub_maps_demo_key')||localStorage.getItem('rh_traffic_key')||'';openModal(`<div class="modalhead"><div><div class="kicker">WEATHER + MAPS</div><h3>Connect live services</h3><p class="caption">Connect once to load live Banswara weather and route estimates.</p></div><button class="round" onclick="closeModal()">×</button></div><div class="field"><label>CONNECTION KEY</label><input id="mapsDemoKey" type="password" value="${esc(existing)}" placeholder="Paste connection key"></div><div id="mapsDemoStatus" class="caption" style="margin-top:10px"></div><div class="modal-actions"><button class="secondary" onclick="localStorage.removeItem('riderhub_maps_demo_key');localStorage.removeItem('rh_traffic_key');$('#mapsDemoStatus').textContent='Connection removed.'">Remove</button><button class="confirm" onclick="saveMapsDemoKey('${esc(after)}')">✓ Connect</button></div>`)}
const baseWeather=window.renderWeather;window.renderWeather=async function(force=false){const el=$('#weatherCard'),k=localStorage.getItem('riderhub_maps_demo_key')||localStorage.getItem('rh_traffic_key')||'';if(!el)return;if(!k){el.innerHTML='<div class="weather-top"><div><div class="kicker">LIVE WEATHER</div><strong>Weather not connected</strong><div class="caption">Connect once to load live Banswara weather.</div></div><button class="secondary" onclick="openMapsDemoSetup(\'weather\')">Connect</button></div>';return}await baseWeather(force);el.querySelectorAll('button').forEach(b=>{if(/Manage Maps Demo Key/i.test(b.textContent))b.textContent='Manage connection'})}
const baseCheckTraffic=window.checkDemoTraffic;window.openTraffic=function(){const k=localStorage.getItem('riderhub_maps_demo_key')||localStorage.getItem('rh_traffic_key')||'';if(!k)return openMapsDemoSetup('traffic');const d=currentDay(),parts=d.segments.filter(s=>!s.backup);openModal(`<div class="modalhead"><div><div class="kicker">TRAFFIC + ETA</div><h3>Current route</h3><p class="caption">Checks the selected route part and uses traffic-aware timing when available.</p></div><button class="round" onclick="closeModal()">×</button></div><div class="field"><label>ROUTE PART</label><select id="trafficPart">${parts.map((s,i)=>`<option value="${i}">${esc(s.label)}</option>`).join('')}</select></div><button class="primary full" onclick="checkDemoTraffic()">Check route now</button><div id="trafficDemoResult" class="alert">Ready.</div><button class="secondary full" style="margin-top:8px" onclick="openMapsDemoSetup('traffic')">Manage connection</button>`)}
if(baseCheckTraffic)window.checkDemoTraffic=async function(){await baseCheckTraffic();const o=$('#trafficDemoResult');if(o)o.innerHTML=o.innerHTML.replace('Traffic-aware ETA returned by Google Routes prototype.','Traffic-aware ETA available.').replace('Basic route estimate returned. Live traffic-aware mode was not available for this request.','Route estimate available. Live traffic timing is not available for this route right now.')}
window.openDocDetail=function(id){const x=DOCS.find(d=>d.id===id);if(!x)return;openModal(`<div class="modalhead"><div><div class="kicker">DOCUMENT</div><h3>${esc(x.name)}</h3><p class="caption">${esc(x.summary)}</p></div><button class="round" onclick="closeModal()">×</button></div><div class="alert">${esc(x.detail)}</div><div class="grid2" style="margin-top:12px"><button class="secondary" onclick="openPrivateDoc('${x.id}')">Open file</button><button class="secondary" onclick="openPrivateDocPicker('${x.id}')">Attach / replace</button></div>${x.id==='hotelBooking'?'<button class="primary full" style="margin-top:8px" onclick="openBookingPdf()">Open booking</button>':''}`)}
window.openDigiLockerInfo=function(){openModal(`<div class="modalhead"><div><div class="kicker">DIGILOCKER</div><h3>COMING SOON</h3></div><button class="round" onclick="closeModal()">×</button></div>`)}
window.openPrivateDocPicker=function(key){const x=DOCS.find(d=>d.id===key);openModal(`<div class="modalhead"><div><div class="kicker">DOCUMENT</div><h3>Attach ${esc(x?.name||'file')}</h3></div><button class="round" onclick="closeModal()">×</button></div><input id="privateDocFile" type="file" accept="${FILE_ACCEPT}" hidden onchange="showPrivateFileName(this)"><div class="file-picker-row"><button class="secondary" onclick="$('#privateDocFile').click()">Choose file</button><span id="privateFileName" class="caption">No file selected</span></div><div class="modal-actions"><button class="secondary" onclick="closeModal()">Cancel</button><button class="confirm" onclick="attachPrivateDoc('${key}')">✓ Save</button></div>`)}
window.attachPrivateDoc=async function(key){const f=$('#privateDocFile')?.files?.[0];if(!f)return toast('Choose a file');await savePrivateDoc(key,f);if(key==='hotelBooking')state.ride.backup.booking=true;save();closeModal();toast(cloudSyncConnected?.()?'File saved and synced':'File saved on this device')}
window.openBookingPdf=async function(){const f=await getPrivateDoc('hotelBooking');if(f){window.open(URL.createObjectURL(f),'_blank');return}if(typeof cloudSyncConnected==='function'&&!cloudSyncConnected()&&riderHubCloudConfig?.().clientId){toast('Reconnect My Account to load the booking from your other device');return}openPrivateDocPicker('hotelBooking')}
window.openNotifications=function(){const d=currentDay(),missed=d.tasks.map((t,i)=>({t,i})).filter(x=>status(x.i)==='upcoming'&&isPastTask(x.t[0],d.date)),p=notificationState();openModal(`<div class="modalhead"><div><div class="kicker">NOTIFICATIONS</div><h3>Ride + maintenance alerts</h3></div><button class="round" onclick="closeModal()">×</button></div><div class="routecard"><strong>Notification status</strong><p>${p==='granted'?'Allowed.':p==='denied'?'Blocked in browser permissions.':p==='default'?'Not enabled yet.':'Not supported in this browser.'}</p></div><div class="routecard"><strong>Chain care</strong><p>${chainKm()} km since last chain care · reminder at 400 · due by 500.</p></div>${missed.length?missed.map(x=>`<div class="routecard"><strong>Missed: ${esc(x.t[1])}</strong><p>${esc(x.t[0])} · Review it before ending the day.</p></div>`).join(''):'<div class="routecard"><strong>No missed ride tasks right now.</strong></div>'}<div class="grid2"><button class="secondary" onclick="requestNotifications()">${p==='denied'?'How to allow':'Enable notifications'}</button><button class="secondary" onclick="reviewDayEnd()">Review day</button></div>`)}
window.renderMore=function(){ensureState();const sync=typeof cloudSyncLabel==='function'?cloudSyncLabel():'Not connected';$('#more').innerHTML=`<div class="card more-hero"><div class="kicker">MORE</div><h1>Rider Hub</h1><p>Account, app controls and utilities.</p></div><div class="more-grid"><button class="more-card" onclick="openMyAccount()"><b>◉</b><span><strong>My Account</strong><small>${esc(sync)}</small></span></button><button class="more-card" onclick="openInstallApp()"><b>↓</b><span><strong>Install Rider Hub</strong><small>Add to your phone or desktop</small></span></button><button class="more-card" onclick="openNotifications()"><b>!</b><span><strong>Notifications</strong><small>Ride and maintenance reminders</small></span></button><button class="more-card" onclick="openMapsDemoSetup()"><b>⌖</b><span><strong>Weather & Maps</strong><small>Connection and route services</small></span></button><button class="more-card" onclick="openTransfer()"><b>⇄</b><span><strong>Backup / Import</strong><small>Export or restore Rider Hub data</small></span></button><button class="more-card" onclick="openEmergency()"><b>SOS</b><span><strong>Emergency contacts</strong><small>112, 108 and custom contacts</small></span></button><button class="more-card" onclick="openStorageInfo()"><b>▤</b><span><strong>Files & storage</strong><small>Documents and cross-device sync</small></span></button><button class="more-card" onclick="openAppSettings()"><b>⚙</b><span><strong>App settings</strong><small>Preferences and permissions</small></span></button><button class="more-card" onclick="openAboutRiderHub()"><b>RH</b><span><strong>About Rider Hub</strong><small>Charcoal Ember · Phase 3</small></span></button></div>`}
window.openMyAccount=function(){const c=typeof riderHubCloudConfig==='function'?riderHubCloudConfig():{},label=typeof cloudSyncLabel==='function'?cloudSyncLabel():'Not connected';openModal(`<div class="modalhead"><div><div class="kicker">MY ACCOUNT</div><h3>${esc(c.name||'Rider Hub')}</h3><p class="caption">${esc(c.email||'Connect Google Drive sync to use the same Rider Hub files on your devices.')}</p></div><button class="round" onclick="closeModal()">×</button></div><div class="routecard"><strong>Cloud sync</strong><p>${esc(label)}${c.lastSync?' · Last sync '+new Date(c.lastSync).toLocaleString():''}</p></div><div class="grid2"><button class="secondary" onclick="openCloudSetup()">Sync settings</button><button class="primary" onclick="riderHubSyncNow()">Sync now</button></div>`)}
window.openStorageInfo=function(){openModal(`<div class="modalhead"><div><div class="kicker">FILES & STORAGE</div><h3>Your files</h3></div><button class="round" onclick="closeModal()">×</button></div><div class="routecard"><strong>Supported files</strong><p>PDF · JPG/JPEG · PNG · WEBP · HEIC/HEIF · TIFF · GIF · BMP · DOC/DOCX · ODT · RTF · TXT</p></div><div class="routecard"><strong>Cross-device</strong><p>${typeof cloudSyncLabel==='function'?esc(cloudSyncLabel()):'Not connected'}</p></div><button class="primary full" onclick="openCloudSetup()">Manage sync</button>`)}
window.openAppSettings=function(){openModal(`<div class="modalhead"><div><div class="kicker">SETTINGS</div><h3>App settings</h3></div><button class="round" onclick="closeModal()">×</button></div><div class="routecard"><strong>Units</strong><p>Metric · km · °C · PSI</p></div><div class="routecard"><strong>Theme</strong><p>Charcoal Ember</p></div><div class="grid2"><button class="secondary" onclick="openNotifications()">Notifications</button><button class="secondary" onclick="openMapsDemoSetup()">Weather & Maps</button></div>`)}
window.openAboutRiderHub=function(){openModal(`<div class="modalhead"><div><div class="kicker">RIDER HUB</div><h3>TVS Ronin · Charcoal Ember</h3><p class="caption">Personal motorcycle OS</p></div><button class="round" onclick="closeModal()">×</button></div><div class="routecard"><strong>Build</strong><p>Phase 3 transition · local-first · offline-ready.</p></div>`)}
window.openInstallApp=async function(){if(installPrompt){installPrompt.prompt();const r=await installPrompt.userChoice;if(r.outcome==='accepted')installPrompt=null;return toast(r.outcome==='accepted'?'Install started':'Install cancelled')}openModal(`<div class="modalhead"><div><div class="kicker">INSTALL</div><h3>Install Rider Hub</h3></div><button class="round" onclick="closeModal()">×</button></div><div class="routecard"><strong>On Android / Chrome</strong><p>Open the browser menu and choose Install app or Add to Home screen.</p></div><div class="routecard"><strong>On desktop Chrome / Edge</strong><p>Use the install icon in the address bar or the browser menu.</p></div>`)}
const oldRenderPage=window.renderPage;window.renderPage=function(page){if(page==='more')renderMore();else if(page==='docs'){setPage('bike',false)}else oldRenderPage(page)}
const oldSetPage=window.setPage;window.setPage=function(page,push=true){ensureState();if(page==='docs')page='bike';if(page==='rides'&&push)state.ui.rideView='list';state.ui.page=page;$$('.page').forEach(p=>p.classList.toggle('active',p.id===page));$$('.navbtn[data-page]').forEach(b=>b.classList.toggle('active',b.dataset.page===page));if(push)history.pushState({page,rideView:page==='rides'?state.ui.rideView:undefined},'',location.pathname+'#'+page);window.scrollTo(0,0);renderPage(page)}
const oldRender=window.render;window.render=function(){ensureState();applyChrome();renderHome();renderBike();renderGear();renderRides();renderMore();updateUndo();$$('.page').forEach(p=>p.classList.toggle('active',p.id===(state.ui.page||'home')));$$('.navbtn[data-page]').forEach(b=>b.classList.toggle('active',b.dataset.page===(state.ui.page||'home')));renderCountdown()}
window.addEventListener('popstate',e=>{const p=e.state?.page||location.hash.slice(1)||'home';if(p==='rides'){state.ui.rideView=e.state?.rideView==='detail'?'detail':'list';setTimeout(renderRides,0)}if(p==='more')setTimeout(renderMore,0)});
ensureState();applyChrome();render();
})();