const fs=require('fs'),vm=require('vm'),assert=require('assert');const code=fs.readFileSync('auth-flow-controller.js','utf8');
function make({transition=false}={}){const listeners={};const shell={classList:{s:new Set(),add(x){this.s.add(x)},remove(x){this.s.delete(x)}},querySelector(sel){if(sel==='#rhAuthStage')return stage;if(sel==='.rh-auth-brand small')return small;return null}};const stage={innerHTML:''},small={textContent:''};const document={querySelector(sel){if(sel==='#rhAuthShell')return shell;if(sel==='#rhAuthStage')return stage;if(sel==='#rhAuthShell .rh-auth-brand small')return small;return null}};const localStorage={getItem(){return null}};const sessionStorage={getItem(){return null}};const window={RIDER_HUB_AUTH_TRANSITION:transition,state:{profile:{}},addEventListener(t,f){(listeners[t]??=[]).push(f)}};const ctx={window,document,localStorage,sessionStorage,setTimeout,clearTimeout,queueMicrotask,console,String,Number,Math,JSON};ctx.globalThis=ctx;vm.createContext(ctx);vm.runInContext(code,ctx);return{window,stage,shell,listeners}}
let t=make();assert(t.stage.innerHTML.includes('Everything around your motorcycle'));t.window.rhApprovedWelcomeNext();assert(t.stage.innerHTML.includes('PRIVATE FILES STAY YOURS'));t.window.rhApprovedWelcomeNext();assert(t.stage.innerHTML.includes('Your bike and rides can follow you'));t.window.rhApprovedWelcomeNext();assert(t.stage.innerHTML.includes('Continue with Google'));
t=make({transition:true});assert(t.stage.innerHTML.includes('Opening your Rider Hub'));console.log('auth-flow-v31 smoke: PASS');
