# Rider Hub Phase 3 intentionally has no paid-cloud credentials.
# Maps Demo Key is pasted by the rider at runtime and stored only in browser localStorage.
# Do not commit API keys, booking PDFs, identity documents, OAuth secrets or tokens.

# Future server-side connectors (for example DigiLocker) must remain disabled until
# a zero-cost deployment option is selected and credentials can be stored securely.
