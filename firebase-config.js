/* Rider Hub Firebase configuration.
   Firebase web config is a public project identifier, not a server secret.
   Fill this object after registering the Rider Hub web app in Firebase Console.
   Never place service-account keys, private keys, OAuth client secrets, or passwords here. */
window.RIDER_HUB_FIREBASE_CONFIG = window.RIDER_HUB_FIREBASE_CONFIG || null;
